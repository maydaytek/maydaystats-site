# Authoring/rendering dependencies (used locally when you run `quarto
# preview` or `quarto render` - NOT needed by the Cloudflare Pages build,
# since freeze:true means the deploy build reuses committed _freeze/
# output instead of re-executing these. See DEPLOY.md.
jupyter>=1.0.0
pandas>=2.0
matplotlib>=3.8
google-cloud-bigquery>=3.25.0
db-dtypes>=1.2.0
pyarrow>=14.0.0
pandas-gbq>=0.26.1
